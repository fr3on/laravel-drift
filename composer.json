{
    "name": "fr3on/laravel-drift",
    "description": "Pre-deploy environment validation & config drift detection for Laravel applications.",
    "keywords": [
        "laravel",
        "env",
        "configuration",
        "validation",
        "drift"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "Ahmed Mardi",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.2",
        "illuminate/support": "^10.0|^11.0|^12.0|^13.0",
        "symfony/console": "^6.0|^7.0|^8.0"
    },
    "require-dev": {
        "pestphp/pest": "^2.0|^3.0",
        "orchestra/testbench": "^8.0|^9.0"
    },
    "autoload": {
        "psr-4": {
            "Fr3on\\Drift\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Fr3on\\Drift\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Fr3on\\Drift\\DriftServiceProvider"
            ],
            "aliases": {
                "Drift": "Fr3on\\Drift\\Facades\\Drift"
            }
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    }
}